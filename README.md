# elevate-labs-internship-tasks
Checked missing values, count missing values, filled missing values using mean,median, and mode. Used label encoding. 
Applied feature scaling using fit transform on age and embarked columns. 
Dropped irrelevant columns like passenger ID, Name and Ticket.